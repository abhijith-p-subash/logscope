## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem being solved. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` and `npm run typecheck:web` pass
- [ ] New behaviour has a test (or there's a reason it doesn't)
- [ ] Docs updated if a command, flag, or config key changed
- [ ] No new runtime dependency (or the PR explains why one is unavoidable)
- [ ] None of the [hard constraints](../docs/development.md#non-negotiable-constraints) are weakened
